# AIGC Wiki - AI图片卡片展示网站

一个展示AI生成图片及其参数的静态网站，可部署在Cloudflare Pages等平台。

## 功能特点

- 🖼️ 响应式卡片网格布局
- 📊 点击查看详细生成参数（模型、采样方法、CFG、VAE、LoRA等）
- 📁 每张卡片独立JSON文档，便于管理
- 🚀 纯静态实现，支持Cloudflare Pages部署

## 目录结构

```
/
├── index.html          # 主页面
├── css/
│   └── style.css       # 样式文件
├── js/
│   └── app.js          # 主逻辑
├── data/
│   └── cards/          # 卡片数据（JSON文件）
├── images/             # 图片资源
│   ├── thumb/          # 缩略图
│   └── full/           # 原图
└── README.md           # 项目说明
```

## 添加新卡片

1. 将图片放入 `images/thumb/` 和 `images/full/` 目录
2. 在 `data/cards/` 目录创建新的JSON文件，格式如下：

```json
{
  "id": "unique-id",
  "title": "图片标题",
  "thumbnail": "images/thumb/xxx.jpg",
  "fullImage": "images/full/xxx.jpg",
  "model": {
    "name": "模型名称",
    "type": "模型类型"
  },
  "parameters": {
    "sampler": "采样方法",
    "cfg": 7.5,
    "steps": 30,
    "vae": "VAE名称",
    "upscaler": "放大算法",
    "seed": 123456789,
    "size": "512x768"
  },
  "loras": [
    { "name": "LoRA名称", "weight": 0.8 }
  ],
  "prompt": "提示词",
  "negativePrompt": "负面提示词",
  "createdAt": "2024-01-01"
}
```

3. 更新 `data/cards/index.json` 添加卡片文件名

## 本地预览

直接用浏览器打开 `index.html` 即可预览，或使用本地服务器：

```bash
# 使用Python
python -m http.server 8080

# 使用Node.js
npx serve
```

## 部署到Cloudflare Pages

1. 将项目推送到GitHub仓库
2. 在Cloudflare Dashboard中创建Pages项目
3. 连接GitHub仓库，选择分支
4. 构建设置留空（纯静态项目）
5. 点击部署

## 技术栈

- HTML5 + CSS3 + JavaScript (ES6+)
- CSS Grid + Flexbox 响应式布局
- 无框架依赖，最小化实现
